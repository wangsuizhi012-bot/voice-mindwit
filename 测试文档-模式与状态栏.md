# voice-assistant · 模式自动识别 + 右上角状态栏 测试文档

> 版本：2026-08-18（叠加在《测试文档.md》之上的新模式/状态栏专项）
> 目的：**验证「系统自动识别 / 语音切换模式 + 右上角实时状态」这套设计是否合理**
> 测试完把结果填到每个用例后面的【 】，有问题贴 `run_assistant.log` 相关行。

---

## 1. 被测设计说明（先对齐预期）

| 项 | 设计 |
|---|---|
| 模式状态机 `MODE` | `listen` 监听中 / `thinking` 理解中 / `command` 指令模式 / `chat` 聊天模式 / `recording` 录制宏 / `skill` 录制技能 / `pending` 待确认 |
| 自动识别 | 每条语音经 ASR→LLM 意图，若 `action == none` 自动进**聊天模式**，否则进**指令模式**；无需手动切换 |
| 语音切换 | 说「聊天模式」强制锁定聊天；说「退出聊天」回到指令/自动识别态 |
| 右上角状态栏 | 置顶小窗移到**屏幕右上角**，窗口顶部芯片按颜色显示当前模式 |
| 颜色编码 | 灰=监听中 / 黄=理解中 / 绿=指令模式 / 蓝=聊天模式 / 橙=录制宏 / 紫=录制技能 / 红=待确认 |

**核心判断点**：在不说任何切换词的前提下，命令和闲聊能否被**自动正确分流**；状态栏颜色与日志 `action` 是否**始终一致**。

---

## 2. 前置条件
- 本地 LLM 已起在 `:1234`；视觉模型 `:1235`（技能训练用，可选）
- 麦克风可用；双击 `run_assistant.bat`
- ⚠️ 本轮改了 **Overlay 位置（左上→右上）** 与 **模式状态机 + 各处状态联动**，**必须重启 bat** 才生效

---

## 3. 测试用例

### A. 启动与位置
| # | 操作 | 预期 |
|---|---|---|
| A1 | 双击 run_assistant.bat | 小窗出现在**屏幕右上角**（非左上），芯片显示「● 监听中」(灰) |
| A2 | 说任意一句话 | 芯片先闪「● 理解中」(黄)，随后切到 绿/蓝 |

【 A1 】通过（实际位置：________） 【 A2 】通过

### B. 自动识别（本次最核心，重点测）
| # | 说 | 芯片预期（颜色） | 行为预期 |
|---|---|---|---|
| B1 | 今天天气怎么样 | ● 聊天模式（蓝） | 走对话，小窗显示回复并朗读 |
| B2 | 打开记事本 | ● 指令模式 · open（绿） | 记事本打开 |
| B3 | 点发送 | ● 指令模式 · click_target（绿） | 自主定位点击 |
| B4 | 保存 | ● 指令模式 · press（绿） | 按 Ctrl+S |
| B5 | 帮我截个图 | ● 指令模式 · screenshot（绿） | 出图+剪贴板 |

【 B1 】【 B2 】【 B3 】【 B4 】【 B5 】
> 通过标准：**全程不说「聊天模式」也能闲聊，命令也不被当闲聊吞掉**。

### C. 语音切换模式
| # | 说 | 芯片预期 | 说明 |
|---|---|---|---|
| C1 | 聊天模式 | ● 聊天模式（蓝） | 锁定聊天，其后语音都走对话（即使像命令也聊） |
| C2 | 今天天气怎么样 | ● 聊天模式（蓝） | 正常闲聊 |
| C3 | 退出聊天 | ● 指令模式（绿） | 恢复自动识别 |
| C4 | 清空对话 | 保持当前模式 | 对话历史清空，芯片不变色 |

【 C1 】【 C2 】【 C3 】【 C4 】

### D. 录制宏
| # | 说 | 芯片预期 |
|---|---|---|
| D1 | 录制一下 测试 | ● 录制宏（橙） |
| D2 | 停止 | ● 监听中（灰），`flows/测试.json` 已生成 |

【 D1 】【 D2 】（日志行：________）

### E. 录制技能
| # | 说 | 芯片预期 |
|---|---|---|
| E1 | 录制技能 点设置 | ● 录制技能（紫） |
| E2 | 完成 | ● 监听中（灰），技能已保存 |

【 E1 】【 E2 】

### F. 待确认态
| # | 操作 | 芯片预期 |
|---|---|---|
| F1 | config 设 `confirm_high_risk:true` 后说「帮我截图发给我」 | ● 待确认（红） |
| F2 | 说「确认」 | 执行，回到 绿/灰 |
| F3 | 说「执行技能 X」 | ● 待确认 · 执行技能确认（红） |
| F4 | 说「取消」 | 取消，回到 灰 |

【 F1 】【 F2 】【 F3 】【 F4 】（F1/F3 可选，取决于配置）

### G. 停止
| # | 操作 | 预期 |
|---|---|---|
| G1 | 说「退出」 | 日志「已退出」，程序停止 |

【 G1 】通过

---

## 4. 设计合理性核查（重点，决定"是否合理"）

| # | 核查项 | 判定标准 | 结果 |
|---|---|---|---|
| R1 | 自动识别误判率 | 命令没被当闲聊（B2~B5 芯片=绿且真执行），闲聊没被当命令（B1 芯片=蓝且走对话） | 【 】 |
| R2 | 状态与日志一致性 | 每条语音期间，芯片颜色 == 日志 `意图：action` 语义一致（如 action=open→绿） | 【 】 |
| R3 | 实时性（线程安全） | ASR 后台线程/监听线程改模式不卡 UI；用 `after(0,...)` 跨线程刷新 | 【 】 |
| R4 | 右上角不被遮挡 | `topmost=True` 且贴右边，不被任务栏/浏览器全屏盖住 | 【 】 |
| R5 | 模式叠加正确 | 录制中(D1)再收到语音被正确吞掉不进 LLM；停止后回到监听(R) | 【 】 |
| R6 | 切换词不串台 | 「退出聊天」只退出聊天不退出程序；「退出」才停程序 | 【 】 |

**合理性结论（填）**：
- 自动识别是否足够？还需要手动「聊天模式」吗？ → ________
- 状态栏颜色是否一眼可辨？有没有和其他 UI 撞色？ → ________
- 是否要加"当前识别文本"也显示在状态栏？ → ________

---

## 5. 验收结论
- [ ] A 小窗在右上角 + 监听中
- [ ] B 自动识别命令/闲聊分流正确（最核心）
- [ ] C 语音可切换聊天/指令
- [ ] D/E 录制态颜色正确且能回到监听
- [ ] F 待确认态红色提示 + 确认/取消生效
- [ ] R1~R6 设计合理性全部通过

未通过项 + 日志片段：
```
（贴这里）
```
