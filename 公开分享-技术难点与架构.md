# 本地语音控制 RPA：张嘴说话，电脑自己截图/点击/打字

> 一套**全本地、零云端**的语音控制方案：你说普通话，本地模型听写 → 本地大模型理解意图 → 脚本自动操作鼠标键盘。识别用 FunASR/SenseVoice，意图理解用本地 llama.cpp 跑的 Qwen，执行用 pyautogui。延迟低于 0.1 秒，隐私全留本机。

---

## 一、它能干啥（效果）

- 说「帮我截个图」→ 截图存盘 + 自动进剪贴板
- 鼠标移到按钮上说「点一下」→ 点在鼠标当前位置（支持双击/右键）
- 说「保存 / 复制 / 粘贴 / 按回车」→ 触发对应键盘热键（Ctrl+S / Ctrl+C / Ctrl+V / Enter）
- 说「打开记事本」→ 启动程序
- 说「退出」→ 停止

整个过程**不需要敲一个字、点一次鼠标**，张嘴就行。识别准确率实测：说「你好」回「你好」，rtf（实时率）0.022——CPU 上 5 秒语音只花 0.39 秒。

---

## 二、技术架构

```
麦克风(实时)
   │  webrtcvad 端点检测（说完自动截断）
   ▼
SenseVoiceSmall 本地转写（FunASR / ModelScope 国内源）
   │  中文文本
   ▼
本地 LLM 意图解析（llama.cpp + Qwen2.5-7B，OpenAI 兼容接口）
   │  JSON 指令：{"action":"screenshot"} / {"action":"click_here"} / ...
   ▼
pyautogui 执行（截图 / 点击 / 按键 / 打字 / 打开）
   │
   ▼
置顶小窗 + 控制台实时回显「听到：xxx / 执行：xxx」
```

**关键设计**：本地大模型**不写任意代码**（那样不可控、易崩），它只把你的口语翻译成固定格式的指令 JSON，真正干活的是脚本里写好的几个动作函数。换一种指令，只需改提示词，不用动架构。

---

## 三、踩过的坑（核心难点，发帖精华）

### 1. 为什么不用 Voice Wake / faster-whisper

- **Voice Wake**：识别率低、输出繁体字、睡眠唤醒不稳定、根本没有截图能力——直接弃用。
- **faster-whisper small**：中文弱、常出繁体。
- **SenseVoiceSmall（阿里达摩院）**：根治中文识别，rtf 0.022，CPU 都无压力。选它没商量。

### 2. Windows bat 编码地狱（每个做 Windows 自动化的都躲不过）

- `UTF-8-BOM + chcp 65001` 组合 → 首行被 BOM 污染 → 报「不是内部或外部命令」
- `echo` 文本里带半角括号 → CMD 把它当命令块闭合 → 报「此时不应有 …」
- Python 生成 bat 时 `\a` 被解析成 BEL(0x07) 控制字符 → 「文件名语法不正确」
- **最终方案**：bat 用 **GBK 无 BOM + 单独 `chcp 65001`**（现代标准做法，根乱码），echo 文本零半角括号。

### 3. torchaudio 的 int16 输入雷（最阴间的一个）

把麦克风录音按 `int16` 喂给模型，torch 2.13 已经**删掉了 int16 的 CPU 随机核**，直接崩：

```
NotImplementedError: "normal_kernel_cpu" not implemented for 'Short'
```

**修复**：输入改成 `float32` 归一化到 `[-1, 1]`，完全匹配 FunASR 官方约定。**不用降 torch、不用重下 1GB**。

### 4. webrtcvad 缺 pkg_resources

`webrtcvad` 取自己版本号时用了 `pkg_resources`，但 **setuptools 81+ 已删除该模块**。装最新 setuptools 也没用。

**修复**：把那两行换成标准库 `importlib.metadata`，不依赖 setuptools。

### 5. 控制台中文乱码

Python 强行设 GBK，跟控制台 UTF-8 对不上 → 满屏乱码。

**修复**：**两端统一 UTF-8**——Python 输出 UTF-8 + bat 里 `chcp 65001`，干净了。

### 6. 麦克风设备选择

笔记本有 20+ 个输入设备（虚拟/真实混在一起），脚本容易选错。

**修复**：直接跟随 **Windows 默认麦克风**（你在系统面板点哪个蓝点，脚本就用哪个）。

### 7. 环境底噪误触发

房间底噪会触发 VAD，但代码 **silently 忽略**这些 `empty speech`，不调 LLM、不执行，无害。

---

## 四、为什么这套比「语音助手 API」香

| 维度 | 云端语音助手 | 本方案 |
|---|---|---|
| 隐私 | 语音上传厂商 | 全本地 |
| 延迟 | 网络往返 | < 0.1 秒 |
| 成本 | 按量付费 | 一次性硬件 |
| 可控性 | 黑盒 | 代码全开源可改 |
| 离线 | 不行 | 断网照跑 |

---

## 五、自主定位点击（鼠标自己飞过去）

说「点发送 / 点确定 / 点设置」，鼠标自己移动到控件中心并点击——你不用先把鼠标移到那里。这是整套方案里最像「魔法」的一步，也是难点最集中的地方。

**核心思路：控件树为主，OCR + 记忆库兜底。**

```
口语 "点发送"
   │  LLM 抽出 target="发送"
   ▼
locate.py 三级命中:
   ① 控件树(uiautomation): 枚举前台窗口控件(名字+类型+坐标)
      目标文字命中控件名 → 取中心坐标 → pyautogui.click
   ② 记忆库(自学习): 之前手动点过的位置, 按当前窗口 rect 还原相对坐标
   ③ OCR 兜底(easyocr): 截全屏识字, 找目标文字 → 中心坐标
```

**为什么不用「截屏 + 视觉大模型」？**
纯视觉方案要把全屏喂给大模型，慢、贵、还得联网或上重型本地模型。而 Windows 标准软件（微信/记事本/浏览器/Office）都暴露 **UI 自动化树**——按钮叫什么、在哪、多大，系统直接给，零额外模型、毫秒级。所以优先吃这口现成饭。

**难点 & 怎么破：**
1. **控件树取不到**：游戏（DirectX）、Electron 自绘控件、老界面没无障碍信息 → 回退 OCR（easyocr 中英文）。
2. **OCR 也找不到**（如记事本「设置」根本没有这个按钮）→ 进**教学态**：小窗提示「请手动点一下」，你把鼠标移过去说「点一下」，系统把该目标在**当前窗口的相对坐标**记进 `click_memory.json`。下次同应用再说「点设置」直接按记忆坐标点，窗口挪动/缩放都不怕。越用越准。
3. **置顶小窗干扰 OCR**：OCR 截全屏会把小窗上「听到的话」也识别成文字 → 截屏前先 `withdraw()` 隐藏小窗，定位完再恢复。

**效果**：标准软件命中率接近 100%；自学习后，连没有文字按钮的「设置」齿轮也能一步命中。

## 六、适用场景 & 局限

**适合**：重复性桌面操作自动化、无障碍辅助、想给老电脑装「语音外挂」的玩家。
**局限**：动作范围受脚本里已实现的函数限制（想加新动作需改代码）；「自主定位」已覆盖标准软件 + 自学习记忆，但纯视觉语义定位（如「点红色按钮」）仍需接 OmniParser / UI-TARS 等视觉模型，本次留作下一步。

---

## 七、发布就绪度（当前状态）

做成本地能跑的版本早已完成，下面是「给别人用」的清单，多数已补齐：

- [x] 路径全部走 `config.json`，无功能相关硬编码（`gen_bat.py` / `启动服务.bat` 旧绝对路径已清理）
- [x] `requirements.txt` 齐全（含 `requests` / `easyocr` / `uiautomation` 等），一键 bat 自动建 venv 并装依赖
- [x] `run_assistant.bat` 双击即跑，首次自动装环境
- [x] `webrtcvad` 的 `pkg_resources` 补丁已做成自动检测
- [x] README 写清：装 FunASR / 起本地 LLM（llama.cpp）/ 跑 bat / 自主定位原理
- [ ] 设备选择「首次运行引导」（当前跟随 Windows 默认麦克风，够用，可选增强）
- [ ] A2A 委派、UI-TARS 视觉定位属「下一步功能」，不影响发布

**结论：已达可发布状态**——clone 后建 venv、起本地 LLM、双击 bat 即可跑。剩下的是锦上添花。
